<%= form_with(model: concert_info, local: true) do |form| %>
  <% if concert_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(concert_info.errors.count, "error") %> prohibited this concert_info from being saved:</h2>

      <ul>
        <% concert_info.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :event %>
    <%= form.text_field :event %>
  </div>

  <div class="field">
    <%= form.label :artist_name %>
    <%= form.text_field :artist_name %>
  </div>

  <div class="field">
    <%= form.label :city %>
    <%= form.text_field :city %>
  </div>

  <div class="field">
    <%= form.label :venue %>
    <%= form.text_field :venue %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
