<p id="notice"><%= notice %></p>

<h1> <%=@artist.name%> </h1>

<%shows = @artist.shows%>

<h1>Shows:</h1>
<% @shows.each do |shows| %>
<h1> <%= shows.location %> </h1>
  <img src="<%= shows.img %>">
<% end %>
<%= link_to 'Back', artists_path %>
